VARIABILES >
    esto x: numerus 5;
    esto y: numerus 10;
    esto z: numerus x + y;
MAIOR >
    z = x + y;
    si (z > 10) {
        >> z;
    } finis;
    per (esto i: numerus 0; i < 3; i++) {
        >> i;
    }
    >> "fin";
FINIS;
